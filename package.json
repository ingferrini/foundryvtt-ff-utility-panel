{
  "name": "wildemount-starter",
  "version": "1.2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "clean": "rimraf dist && mkdir -p dist"
  },
  "devDependencies": {
    "@league-of-foundry-developers/foundry-vtt-types": "^10.312.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0",
    "vite": "^5.0.0"
  }
}